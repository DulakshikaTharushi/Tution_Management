<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/main"
    android:background="#FFFFFF">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#007bff"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/logo_lion"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/location"
            android:layout_marginEnd="8dp"
            android:contentDescription="Location Icon" />

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="My Account"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- My Account button -->
    <Button
        android:id="@+id/button_my_account"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="My Account"
        android:background="#333333"
        android:textColor="#FFFFFF"
        android:layout_margin="16dp"
        android:layout_below="@id/header" />

    <!-- Vertical list of buttons -->
    <LinearLayout
        android:id="@+id/vertical_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_below="@id/button_my_account"
        android:layout_margin="16dp">

        <Button
            android:id="@+id/button_my_profile"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="My Profile"

            android:drawablePadding="8dp"
            android:padding="16dp"
            android:background="#FFFFFF"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/button_my_courses"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="My Results"

            android:drawablePadding="8dp"
            android:padding="16dp"
            android:background="#FFFFFF"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/button_my_payments"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Schedule"

            android:drawablePadding="8dp"
            android:padding="16dp"
            android:background="#FFFFFF"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/button_my_qr"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="My QR Code"
            android:drawablePadding="8dp"
            android:padding="16dp"
            android:background="#FFFFFF"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />
    </LinearLayout>

</RelativeLayout>
